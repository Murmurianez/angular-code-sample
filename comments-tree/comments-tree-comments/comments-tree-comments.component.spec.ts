import { TestBed, async } from '@angular/core/testing';
import { RouterTestingModule } from '@angular/router/testing';
// import { AppComponent } from './app.component';

describe('AnswersComponent', () => {
  // beforeEach(async(() => {
  //   TestBed.configureTestingModule({
  //     imports: [
  //       RouterTestingModule
  //     ],
  //     declarations: [
  //       AppComponent
  //     ],
  //   }).compileComponents();
  // }));

  it('Комментарии начиная со 2 уровня отображаются в дочернем блоке', () => {
    // const fixture = TestBed.createComponent(AppComponent);
    // const app = fixture.debugElement.componentInstance;
  });

  it('При ответе, имя предыдущего комментатора пишется только для комментариев начиная от 3 уровня', () => {
    // const fixture = TestBed.createComponent(AppComponent);
    // const app = fixture.debugElement.componentInstance;
  });

  it('Большой счётчик повышается', () => {
    // const fixture = TestBed.createComponent(AppComponent);
    // const app = fixture.debugElement.componentInstance;
  });

  it('Большой счётчик понижается', () => {
    // const fixture = TestBed.createComponent(AppComponent);
    // const app = fixture.debugElement.componentInstance;
  });

  it('При клике на кнопку "Ответ", появляется блок ответа', () => {
    // const fixture = TestBed.createComponent(AppComponent);
    // const app = fixture.debugElement.componentInstance;
  });

});
